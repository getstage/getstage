# Project AI domain helpers

Pure TypeScript helpers for `projectAi` — **no** `query` / `mutation` / `action` exports.

Convex endpoints are registered only in `convex/projectAi.ts`. Handlers live in `convex/lib/projectAi/handlers/`.
