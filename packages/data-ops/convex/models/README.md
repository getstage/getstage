# Convex models

TypeScript types and Convex `v.*` validators shared across handlers.

- **`models/projects/`** — project/phase validators for UI and API handlers
- **`models/integrations/`** — OAuth, sheets, stripe, stitch integration types

REST Zod schemas for Hono live in **`convex/api/models.ts`** (external HTTP contract).
